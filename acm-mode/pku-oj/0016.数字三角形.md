# P0870:数字三角形
[题目链接](http://cxsjsxmooc.openjudge.cn/dspythonbookqh2/P0870/)

- 🚀 动态规划：用空间换时间
---
### 自顶向下---递归解决
>用二维数组D存储数字三角形的值
```C
int D[101][101];
```
>定义函数MaxSum(int row, int j);
```C
int MaxSum(int row, int j);
//返回从第row行的第j个数到最底部的最大值
```
- 递归
```C
int MaxSum(int row, int j)//返回第row行到底边的最大和
{
	if (row == N) //已在最底部，直接返回
		return D[row][j];
  //其正下方的点到底边的最大值
	int x = MaxSum(row + 1, j);
  //其正下方右边的点到底边的最大值
	int y = MaxSum(row + 1, j + 1);
	return max(x, y) + D[row][j];
}
```
>函数没有错
>但是时间复杂度是 $2^{n}$ $2^{100}$  就算是宇宙毁灭了也算不出来呀
>
>因为涉及了大量的重复计算
>
>那怎么办呢
>
>我们要避免重复计算
>
>我们算出一个值之后就把它记下来，以后要用就拿出来就行了
```C
int max_sum[101][101];
//用一个二维数组记录每个点到底部的最大和
//我们把数组的值初始化为-1，用于判断是否已经计算过了
```
- 📝优化后的MaxSum()函数
```C
int MaxSum(int row, int j)//返回第row行到底边的最大和
{
	if (max_sum[row][j] != -1)//若已经有记录，则直接拿出来用，不用再计算
		return max_sum[row][j];
	if (row == N)//若点刚好在底部，则直接将其值记录进数组
		max_sum[row][j] = D[row][j];
	else
		max_sum[row][j] = max(MaxSum(row + 1, j), MaxSum(row + 1, j + 1))
		+ D[row][j];
	return max_sum[row][j];
}
```
>我们只要算出所有点到底部的最大值就行了，于是时间复杂度是 $O(n^2)$
>
>因为点的个数是 $\frac{n(n+1)}{2}$
### 递归完整代码
### ***C:***
```C
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#define max(a,b) ((a) > (b) ? (a) : (b))
D[101][101];//第row行第j个数的值
max_sum[101][101];//第row行第j个数到底边的最大值
int N;//总的行数
int MaxSum(int row, int j)//返回第row行到底边的最大和
{
	if (max_sum[row][j] != -1)
		return max_sum[row][j];
	if (row == N)
		max_sum[row][j] = D[row][j];
	else
		max_sum[row][j] = max(MaxSum(row + 1, j), MaxSum(row + 1, j + 1))
		+ D[row][j];
	return max_sum[row][j];
}

int main(void)
{
	scanf("%d", &N);
	int i, j;
	for (i = 0; i < N; i++)
	{
		for (j = 0; j <= i; j++)
		{
      //读入每个点的值
			scanf("%d", &D[i][j]);
      //将记忆数组的值初始化为-1
			max_sum[i][j] = -1;
		}
	}
	printf("%d", MaxSum(0, 0));
}
```
---
### 自底向上--循环解决
>从边界状态出发  
>从底部往上递推  
>用一个二维max_sum数组存子问题的解
```C
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#define max(a,b) ((a) > (b) ? (a) : (b))
#define MAX 101
int D[MAX][MAX];
int max_sum[MAX][MAX];
int main(void)
{
	int rows;
	scanf("%d", &rows);
	int i, j;
	for (i = 0; i < rows; i++)
	{
		for (j = 0; j <= i; j++)
			scanf("%d", &D[i][j]);
	}

	//边界状态：最底部那一行
	for (i = 0; i < rows; i++)
	{
		max_sum[rows - 1][i] = D[rows - 1][i];
	}
	//从底部出发，人人为我递推
	for (i = rows - 2; i >= 0; i--)
	{
		for (j = 0; j <= i; j++)
		{
			//转移方程:
			max_sum[i][j] = D[i][j] +
				max(max_sum[i + 1][j], max_sum[i + 1][j + 1]);
		}
	}
	printf("%d", max_sum[0][0]);
}
```
- 空间优化
>将max_sum变为一个一维的数组
```C
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#define max(a,b) ((a) > (b) ? (a) : (b))
#define MAX 101
int D[MAX][MAX];
int max_sum[MAX];
int main(void)
{
	int rows;
	scanf("%d", &rows);
	int i, j;
	for (i = 0; i < rows; i++)
	{
		for (j = 0; j <= i; j++)
			scanf("%d", &D[i][j]);
	}
	//状态的一个边界
	for (i = 0; i < rows; i++)
		max_sum[i] = D[rows - 1][i];
	//人人为我递推
	for (i = rows - 2; i >= 0; i--)
	{
		for (j = 0; j <= i; j++)
			max_sum[j] = D[i][j] +
			max(max_sum[j], max_sum[j + 1]);
	}
	printf("%d", max_sum[0]);
}
```
- 空间再优化
>直接利用D数组
```C
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
#define max(a,b) ((a) > (b) ? (a) : (b))
#define MAX 101
int D[MAX][MAX];

int main(void)
{
	int rows;
	scanf("%d", &rows);
	int i, j;
	for (i = 0; i < rows; i++)
	{
		for (j = 0; j <= i; j++)
			scanf("%d", &D[i][j]);
	}
	
	//人人为我递推
	for (i = rows - 2; i >= 0; i--)
	{
		for (j = 0; j <= i; j++)
			D[i][j] = D[i][j] +
			max(D[i + 1][j], D[i + 1][j + 1]);
	}
	printf("%d", D[0][0]);
}
```
